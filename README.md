# RabbitMQ Example
- Run a RabbitMQ container using the following command: `docker run -d --name rabbitmq -p 5672:5672 -p 15672:15672 rabbitmq:4-management`
- The simple consumer and publisher examples can be used to test sending and receiving messages
